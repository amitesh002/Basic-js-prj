# 🧠 JavaScript Basic Projects

This repository contains **three beginner-friendly JavaScript projects** designed to build a strong foundation in DOM manipulation, timing functions, and user interaction using HTML, CSS, and JavaScript.

---

## 🚀 Projects Overview

### 1️⃣ Simple Counter
A basic app that lets you **increase, decrease, and reset** a counter value.

#### 🧩 Features
- Increment and decrement buttons  
- Reset to zero  
- Real-time value update on the screen  

#### 🛠️ Concepts Used
- DOM manipulation (`querySelector`, `innerHTML`)
- Event handling (`addEventListener`)
- Basic variable operations


---

### 2️⃣ Digital Real-Time Clock
A **live digital clock** that shows the current hours, minutes, and seconds — updated every second.

#### 🧩 Features
- Displays system time in HH:MM:SS format  
- Real-time updates using `setInterval()`  
- Responsive layout with clean UI  

#### 🛠️ Concepts Used
- `Date()` object for real-time data  
- String formatting with `padStart()`  
- Flexbox and responsive CSS design  


---

### 3️⃣ Pomodoro Timer 🍅
A **focus timer app** based on the Pomodoro technique — 25 minutes of focus followed by a 5-minute rest.

#### 🧩 Features
- Start, Pause, and Reset buttons  
- Auto-switch between focus and rest sessions  
- Displays countdown in MM:SS format  
- Keeps the timer state consistent during pause/reset  

#### 🛠️ Concepts Used
- `setInterval()` and `clearInterval()`  
- Conditional logic and countdown timer  
- DOM updates and event handling  
- Modular and maintainable JS structure  

---

## 🧰 Technologies Used
- **HTML5** – Structure of the web page  
- **CSS3** – Styling and layout (responsive design)  
- **JavaScript (ES6)** – Core logic and interactivity  

---

## 🧠 Learning Outcomes
By completing these projects, you’ll understand:
- How to manipulate the DOM dynamically  
- How to handle user input through events  
- How to use JavaScript timing functions (`setInterval`, `clearInterval`)  
- How to structure small web projects efficiently  

---
 
